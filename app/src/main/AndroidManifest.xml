<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.group9.fooddelivery">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fooddelivery">
        <activity android:name=".ui.Order.OrderDetail"></activity>
        <activity android:name=".ui.Order.Order_Settlement" />
        <activity android:name=".ui.address.updateaddress" />
        <activity android:name=".ui.address.add_address" />
        <activity android:name=".ui.address.address_list" />
        <activity android:name=".ui.menu.Menudelivery" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>