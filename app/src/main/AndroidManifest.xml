<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.group9.fooddelivery">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".context.FoodDeliveryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fooddelivery">
        <activity
            android:name=".ui.register.ui.register.RegisterActivity"
            android:label="@string/title_activity_register"></activity>
        <activity
            android:name=".ui.login.ui.login.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".ui.order.OrderDetailActivity" />
        <activity android:name=".ui.order.OrderSettlementActivity" />
        <activity android:name=".ui.address.AddressUpdatingActivity" />
        <activity android:name=".ui.address.AddressAddingActivity" />
        <activity android:name=".ui.address.AddressListActivity" />
        <activity android:name=".ui.foodmenu.FoodMenuActivity" />
    </application>

</manifest>